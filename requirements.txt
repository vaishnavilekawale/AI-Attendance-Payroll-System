# Flask and Web Framework
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-WTF==1.2.1
WTForms==3.1.1
Flask-Limiter==3.5.0

# Database
SQLAlchemy==2.0.23
PyMySQL==1.1.0

# AI and Computer Vision
# NOTE: opencv-contrib-python is a superset of opencv-python (same `cv2`
# namespace, plus the "contrib" modules). Installing both in the same
# environment creates two competing installs of `cv2`; which one "wins"
# depends on install order rather than being a defined, reproducible
# state, and this has been a real source of "works on my machine" build
# failures. Keep ONLY opencv-contrib-python.
opencv-contrib-python==4.8.1.78
deepface==0.0.79
mediapipe==0.10.21
numpy==1.26.2
tensorflow==2.15.0
tf-keras==2.15.1

# Image Processing
Pillow==10.1.0

# PDF Generation
reportlab==4.0.7
qrcode==7.4.2
pikepdf==9.4.2

# Email
python-dotenv==1.0.0

# Security
Werkzeug==3.0.1
cryptography==43.0.3

# Utilities
python-dateutil==2.8.2

# Scheduler for automatic payroll generation
APScheduler==3.10.4

# Development (optional)
pytest==7.4.3
pytest-flask==1.3.0

